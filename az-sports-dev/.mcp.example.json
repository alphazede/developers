{
  "mcpServers": {
    "alphazede-sports": {
      "command": "npx",
      "args": [
        "-y",
        "@azs/mcp-server",
        "serve"
      ],
      "env": {
        "API_KEY": "${API_KEY}"
      }
    }
  }
}
