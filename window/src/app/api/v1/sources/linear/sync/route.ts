import type { LinearTaskAdapter } from "../../../../../../adapters/linear";
import { hasExactKeys, readBoundedJsonObject, RequestBoundaryError } from "../../../request-boundary";
import { getLiveConnectorRuntime } from "../../../../../../server/connectors/live-runtime";

export type LinearRouteAuthorization=(request:Request)=>boolean;
export const createLinearSyncHandler=(adapter:LinearTaskAdapter,authorize:LinearRouteAuthorization)=>async(request:Request)=>{try{if(!authorize(request))return Response.json({error:{code:"UNAUTHORIZED"}},{status:401,headers:{"cache-control":"private, no-store"}});const body=await readBoundedJsonObject(request,4_096);if(!hasExactKeys(body,["consentRevision","fetchedAt","estimateRule"]))throw new Error("invalid");const result=await adapter.sync(body as Parameters<LinearTaskAdapter["sync"]>[0]);return Response.json(result,{status:result.ok?200:result.error.code==="AUTH_REQUIRED"?401:result.error.code==="SCOPE_DENIED"?403:502,headers:{"cache-control":"private, no-store"}});}catch(error){const status=error instanceof RequestBoundaryError?error.status:400;return Response.json({error:{code:status===413?"PAYLOAD_TOO_LARGE":"LINEAR_SYNC_REJECTED"}},{status,headers:{"cache-control":"private, no-store"}});}};
const disabled=()=>Response.json({error:{code:"LINEAR_SOURCE_DISABLED",message:"Linear connection is not configured.",retriable:false}},{status:503,headers:{"cache-control":"private, no-store"}});
export const POST=async(request:Request)=>{const runtime=await getLiveConnectorRuntime();if(!runtime?.config.linear)return disabled();const command=await runtime.authorizeMutation(request);if(!command)return Response.json({error:{code:"UNAUTHORIZED"}},{status:401,headers:{"cache-control":"private, no-store"}});try{const adapter=await runtime.linearAdapter(),facade={sync:async(input:Parameters<LinearTaskAdapter["sync"]>[0])=>{const result=await adapter.sync(input);if(result.ok)await runtime.store.commitConnectorSync({...command,source:"linear",consentRevision:input.consentRevision,freshness:result.freshness,tasks:result.tasks});return result;}} as LinearTaskAdapter;return createLinearSyncHandler(facade,()=>true)(request);}catch{return disabled();}};
