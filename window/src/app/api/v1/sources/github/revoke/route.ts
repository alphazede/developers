import type { ConnectorPrivacyService } from "../../../../../../application/connectors";
import { hasExactKeys, readBoundedJsonObject, RequestBoundaryError } from "../../../request-boundary";
import { getLiveConnectorRuntime } from "../../../../../../server/connectors/live-runtime";

export type GitHubRouteAuthorization=(request:Request)=>boolean;
export const createGitHubRevokeHandler=(service:ConnectorPrivacyService,authorize:GitHubRouteAuthorization)=>async(request:Request)=>{try{if(!authorize(request))return Response.json({error:{code:"UNAUTHORIZED"}},{status:401,headers:{"cache-control":"private, no-store"}});const body=await readBoundedJsonObject(request,4_096);if(!hasExactKeys(body,["consentRevision"]))throw new Error("invalid");return Response.json(await service.revoke("github",body.consentRevision as number),{headers:{"cache-control":"private, no-store"}});}catch(error){const status=error instanceof RequestBoundaryError?error.status:400;return Response.json({error:{code:status===413?"PAYLOAD_TOO_LARGE":"GITHUB_REVOCATION_REJECTED"}},{status,headers:{"cache-control":"private, no-store"}});}};
const disabled=()=>Response.json({error:{code:"GITHUB_SOURCE_DISABLED",message:"GitHub App connection is not configured.",retriable:false}},{status:503,headers:{"cache-control":"private, no-store"}});
export const POST=async(request:Request)=>{const runtime=await getLiveConnectorRuntime();if(!runtime?.config.github)return disabled();const command=await runtime.authorizeMutation(request);if(!command)return Response.json({error:{code:"UNAUTHORIZED"}},{status:401,headers:{"cache-control":"private, no-store"}});const service={revoke:(_source:string,revision:number)=>runtime.privacy.revoke("github",revision,command)} as ConnectorPrivacyService;return createGitHubRevokeHandler(service,()=>true)(request);};
