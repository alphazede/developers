# Capacity scheduling prototype

This unpublished prototype uses deterministic synthetic data and requires no credentials.

Run `pnpm dev` for local development or `pnpm verify` for the release-parity checks. Distributed source is licensed under [Apache-2.0](LICENSE).
